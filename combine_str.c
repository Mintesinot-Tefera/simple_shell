#include "lsh_shell.h"

/**
 * conc_str - ncatenates two strings
 *
 * @dest: Destination string
 *
 * @src: Origin string
 * Return: Always 0
 */

char *conc_str(char *dest, char *src)
{
	int i, j = 0;

	for (i = 0; d[i] != '\0'; i++)
	{
	}
	for (j = 0; src[j] != '\0'; j++)
	{
		d[i] = src[j];
		i++;

	}
	d[i] = '\0';
return (d);
}

